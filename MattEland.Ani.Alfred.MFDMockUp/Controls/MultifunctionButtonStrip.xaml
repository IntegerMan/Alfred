<UserControl x:Class="MattEland.Ani.Alfred.MFDMockUp.Controls.MultifunctionButtonStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Name="mfdButtonStrip"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch"
             HorizontalContentAlignment="Center"
             VerticalAlignment="Stretch"
             VerticalContentAlignment="Center"
             d:DesignHeight="50" d:DesignWidth="300">
    
    <StackPanel Orientation="{Binding Path=Orientation, 
                                      ElementName=mfdButtonStrip, 
                                      FallbackValue=Horizontal}">

        <Button Style="{StaticResource MFDButtonStyle}" />

        <Button Style="{StaticResource MFDButtonStyle}" />

        <Button Style="{StaticResource MFDButtonStyle}" />

        <Button Style="{StaticResource MFDButtonStyle}" />

        <Button Style="{StaticResource MFDButtonStyle}" />
        
    </StackPanel>
    
</UserControl>
