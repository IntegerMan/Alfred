<Window x:Class="MattEland.Ani.Alfred.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:MattEland.Ani.Alfred.Core;assembly=MattEland.Ani.Alfred.Core"
        mc:Ignorable="d"
        Loaded="OnWindowLoaded"
        Title="{Binding Path=NameAndVersionString}" Height="600" Width="800" 
        Closing="OnWindowClosing"
        d:DataContext="{d:DesignInstance core:AlfredProvider}">
    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Click="OnExitClicked" />
            </MenuItem>
            <MenuItem Header="_Alfred">
                <MenuItem Header="_Initialize" Click="OnInitializeClicked" />
                <MenuItem Header="_Shutdown" Click="OnShutdownClicked" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding NameAndVersionString}" />
                    <TextBlock Text="Status:" />
                    <TextBlock Text="{Binding Path=Status}" Margin="8,0" FontWeight="Bold" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <DockPanel DockPanel.Dock="Left" Width="300" Margin="8">
            <TextBlock DockPanel.Dock="Top">Console Events</TextBlock>
            <ListBox x:Name="EventsList" ItemsSource="{Binding Path=Console.Events}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1" Margin="8,4,8,0">
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Path=Time, StringFormat=T}" />
                                    <TextBlock Text="-" Margin="4,0" />
                                    <TextBlock Text="{Binding Path=Title}" />
                                </StackPanel>
                                <StackPanel Margin="8, 4">
                                    <TextBlock Text="{Binding Message}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <StackPanel>
            <TextBlock>This area will hold widgets</TextBlock>
        </StackPanel>

    </DockPanel>
</Window>
